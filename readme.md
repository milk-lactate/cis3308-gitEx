# cis3308

this is a git example

there has been a change woag

to commit a file, use git add fileName, then
you can use git commit -m "message" to commit
the changes to the repository

the most important cmds are
git init
git add <fileName>
git commit -m "message here"
git pull <remote> <branch>
git push <remote> <branch>

a remote is a copy of repo that lives somewhere else
    in this case it lives on github

this is a change made on friday 04 9 26